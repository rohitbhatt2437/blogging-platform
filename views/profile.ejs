<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="flex w-full justify-end ">
            <a class="text-sm bg-red-500 rounded-md px-3 py-2" href="/logout">logout</a>
        </div>
        <h3 class="text-3xl mb-2">Hello,<%= user.username %></h3>
        <h5 class="mb-4">you can create a new user</h5>
        <form method="post" action="/post">
            <textarea placeholder="what's on your mind" name="content" id="" class="mb-2 block p-3 w-1/3 outline-none resize-none bg-transparent border-2 border-zinc-800 rounded-md"></textarea>
            <input class="text-sm px-3 py-2 bg-blue-500 block rounded-md w-40" type="submit" value="create new post">
        </form>

        <div class="posts mt-20">
            <h3 class="text-zinc-500">your posts</h3>
            <div class="postcontainer mt-4">
                <%user.posts.reverse().forEach(function(post){ %>
                    <div class="post w-1/3 p-4 rounded-md mb-4 border-2 border-zinc-800">
                    <h4 class="text-blue-500 mb-2"><%=user.username%></h4>
                    <p class="text-sm tracking-tight"><%=post.content%></p>
                    <div class="btns flex gap-5 mt-5">
                        <a class="text-blue-500" href="">Like</a>
                        <a class="text-zinc-700" href="">edit</a>
                    </div>
                </div>
                <%})%>
            </div>
        </div>
        
    </div>
</body>
</html> 